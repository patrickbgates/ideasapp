<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Idea App</title>
        <link rel="stylesheet" href="styles.css">
        
    </head>
    <body>
        <h1>Now That's a Great Idea!</h1>
        <div>
            <h2>Submit a New Idea</h2>
            <form action="/idea" method="POST">
                <fieldset>
                    <div class="formRow">
                        <label for="name">Name</label>
                        <input type="text" placeholder="Enter your name" id="name" name="name" autofocus>
                    </div>
                    <div class="formRow">
                        <label for="idea">Idea</label>
                        <textarea placeholder="Enter an idea" id="idea" name="idea"></textarea>
                    </div>
                    <div class="formRow">
                        <button type="submit">Submit</button>
                    </div>
                </fieldset>
            </form>
        </div>
        <div>
            <h2>Existing Ideas</h2>
            <ul>
            <% for (var i = 0; i < ideas.length; i++) { %>
                <li>
                    <p>&quot;<%= ideas[i].idea %>&quot;</p>
                    <cite><%= ideas[i].name %></cite>
                </li>
            <% } %>
            </ul>
        </div>
    </body>
</html>